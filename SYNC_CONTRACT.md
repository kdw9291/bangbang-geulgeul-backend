# 동기화 계약 — S3 N1

앱과 서버가 함께 지키는 규칙의 **단일 원본**이다. 코드를 쓰기 전에 정한다.

- 작성 2026-08-21 · Codex 31회차 xhigh 설계 검토 2회 반영
- 관련: [README.md](README.md)(저장 스키마·ID 계약), `PROJECT_BRIEF.md`, `PROJECT_STATUS.md`

## 왜 이 문서가 따로 있는가

"동기화" 라는 한 단어에 **성격이 다른 세 가지**가 섞여 있었다. 섞어 두면 한쪽
규칙이 다른 쪽을 망가뜨린다. 이름을 나눈다.

| 이름 | 언제 | 성격 |
|---|---|---|
| **로그인 정산**(bootstrap reconciliation) | 로그인하는 순간 **한 번** | 합치지 않는다. 한쪽을 고른다 |
| **평시 동기화**(steady-state sync) | 로그인 상태에서 계속 | **ID 별**로 보낸다. 통째로 덮지 않는다 |
| **카탈로그 이행**(catalog migration) | 행정구역이 바뀔 때 | 옛 ID 를 보존하고 표시에서만 뺀다 |

로그인 정산이 비병합인 것은 사용자 결정이다. **그 규칙을 평시 동기화로 가져오면
안 된다** — 두 기기에서 긁으면 늦게 도착한 쪽이 앞의 것을 지운다.

---

## 1. 역할

| | 무엇의 원본인가 |
|---|---|
| **서버** | **동기화가 끝난 계정 데이터**의 기준 원본 |
| **로컬** | 계정별 **내구성 캐시** + **미전송 큐**(outbox) |

**로컬 저장을 없애지 않는다.** 검토했고 세 가지 이유로 유지한다(2026-08-21).

1. 여행 앱이라 산·해안·해외에서 쓴다. 네트워크가 없을 때 못 긁으면 핵심 기능이 죽는다
2. **M1 계약이 "저장에 성공해야 수집이 성립한다"** 이고 긁기 화면이 그 future 를
   기다린다. 서버 왕복으로 바꾸면 네트워크 단절·토큰 만료·서버 장애가 전부
   "지역을 긁을 수 없음" 이 된다
3. 앱을 켜자마자 지도를 칠할 수 없게 된다

**"서버가 원본" 과 "로컬에도 둔다" 는 충돌하지 않는다.** 서버가 원본인 것은
*동기화가 끝난* 데이터에 대해서다. 아직 못 보낸 것은 **보류 중 변경**이며
서버 데이터를 받아도 **지우지 않는다**(4.4 참고).

## 2. 긁기 완료의 정의 — 바꾸지 않는다

```
긁기 완료 = 내구성 있는 로컬 저장에 성공한 시점
```

- **게스트**: 게스트 스냅샷 원자 저장. **outbox 를 만들지 않는다** — 보낼 곳이 없다
- **로그인 상태**: 계정 스냅샷 + **그 계정의** outbox 를 **한 파일에 원자 저장**

서버 전송은 그 **뒤에** 비동기로 한다. `onCollected` 는 서버를 기다리지 않는다.

### 2.1 로컬 저장 형식

앱에 DB 를 넣지 않는다. 193개 규모라 **기존 JSON 원자 교체를 확장**한다.
계정별로 파일 하나에 `{owner, snapshot, outbox, lastServerRevision}` 를 담는다.

`owner` 를 파일 안에 두는 이유는 **엉뚱한 계정 파일을 읽었을 때 조용히 넘어가지
않기 위해서**다.

---

## 3. 상태

`guest` = 비로그인. `account/<userId>` = 계정별 영역.

| 상태 | 뜻 |
|---|---|
| `guest` | 로그인하지 않았다 |
| `account-active` | 로그인했고 토큰이 살아 있다 |
| `account-auth-required` | **토큰이 만료됐지만 계정 영역은 그대로다.** 게스트로 내리지 않는다 |
| `switching-account` | 정산 중. 3.3 의 단계를 거친다 |
| `deleting-account` | 계정 삭제 중 |

**토큰이 만료돼도 계속 긁을 수 있다.** 계정 캐시와 outbox 에 쌓고
`account-auth-required` 로 표시한다. 산에서 토큰이 만료됐다고 기록을 잃으면 안 된다.

### 3.1 전이

| 상황 | 동작 |
|---|---|
| 비로그인 | `guest` 로 즉시 쓴다. 서버를 부르지 않는다 |
| **빈 계정에 로그인** | 게스트를 계정에 **원자적으로** 올린다. 성공하면 활성 게스트를 비운다 |
| **데이터 있는 계정에 로그인** | 3.3 의 정산 절차를 거친다 |
| **양쪽 다 0건** | `initialize-if-empty` 를 **부르지 않는다.** 계정만 활성화한다 |
| 로그인 중 긁기 | 계정 스냅샷 + outbox 원자 저장 → 즉시 완료 → 뒤에서 전송 |
| 로그아웃 | 토큰 폐기 · 서버 세션 해지 → `guest`. **계정 캐시를 게스트에게 보여주지 않는다** |
| 미전송이 남은 채 로그아웃 | **그 계정 영역에 보존**하고 경고한다. 게스트나 다른 계정으로 옮기지 않는다 |
| 다른 계정 로그인 | 이전 계정 데이터를 **한 프레임도 보여주지 않고** 새 영역을 읽는다 |
| 계정 삭제 | 8절 |

**모든 요청에 `userId + sessionEpoch` 를 붙인다.** 로그아웃 뒤 늦게 도착한 A 계정
응답이 B 계정 영역에 반영되면 안 된다. 응답은 **보낼 때의 영역에만** 적용한다.

### 3.2 "계정이 비어 있다" 의 정의

**수집 레코드가 0건인 것**이다. 사용자 행이 있는지와 무관하다.

판정은 **트랜잭션 안에서 `NOT EXISTS collection_record`** 로 한다.
`collectionRevision` 은 변경 감지용이며 **빈 상태 판정에 겸용하지 않는다** — 빈
스냅샷을 올려 revision 만 올라가면 "0건인데 초기화 불가" 인 계정이 생긴다.

```
POST /collections:initialize-if-empty
  조건: 그 계정에 collection_record 가 하나도 없다 (한 트랜잭션)
  성공: 삽입 + revision 증가
  실패: 409 + 서버 현재 상태 → 호출자는 3.3 으로 간다
  (userId, operationType, idempotencyKey) 가 unique 이고 payload hash 를 함께 둔다
  같은 키 · 같은 payload 재호출 = 이전 결과 반환
  같은 키 · 다른 payload = 오류
```

`idempotencyKey` 는 **요청을 보내기 전에 로컬에 저장**한다. 응답만 유실됐을 때
같은 키로 재시도해야 중복 삽입이 안 생긴다.

### 3.3 정산 절차 — 순서가 계약이다

**사용자 결정(2026-08-21): 복구본을 남기지 않고 로컬을 완전히 지운다.**

내가 30일 복구본을 제안했고 Codex 도 Critical 로 권했으나, 사용자가 **경고만 하고
완전 삭제**를 선택했다. **수집 기록 34곳을 잃은 사고 전례**를 알린 뒤의 결정이다.

복구본이 없으므로 **순서가 유일한 방어선**이다. 반드시 이 순서로 한다.

```
1. 게스트 스냅샷 해시와 서버 collectionRevision 을 고정한다
2. 그 둘로 차이를 계산해 경고 화면을 만든다
3. 확인 직전에 게스트 해시를 다시 확인한다 — 달라졌으면 취소하고 1 로 돌아간다
4. 계정 스냅샷을 내려받아 **검증하고 계정 캐시에 원자 저장한다**
5. 그 다음에 게스트를 삭제하고 활성 영역을 바꾼다
```

**4 와 5 의 순서를 바꾸면 안 된다.** 지우고 나서 받다가 네트워크가 끊기거나 앱이
죽으면, 사용자가 받아들인 "덮어쓰기" 보다 **더 큰 손실**이 난다. 경고는 선택
실수만 줄일 뿐 이 장애는 못 막는다.

**전환 journal** 을 둔다. 담는 것은 `{operationId, guestSnapshotHash,
serverCollectionRevision, targetUserId, phase}` 뿐이다. **삭제된 레코드 본문을
담지 않는다** — 담으면 그것이 복구본이 되어 사용자 결정과 어긋난다.

### 3.4 경고 화면 — 유일한 안전장치

확인을 받기 전에는 **아무것도 지우지 않는다.** 반드시 보여준다.

- 기기에 있는 곳 수 / 계정에 있는 곳 수
- **기기에만 있어 사라질 곳 수** (가장 크게)
- 같은 지역인데 **메모가 다른** 곳 수
- 같은 지역인데 **수집 날짜가 다른** 곳 수 — 서버 것이 이기므로 날짜도 바뀐다
- "되돌릴 수 없습니다"
- 기본 버튼은 **취소**

### 3.5 로그인 성공 후 계정 확인

OAuth 가 이전 계정을 자동 선택하는 일이 흔하다. **정산 전에** 어떤 계정인지 보여주고
취소할 수 있게 한다.

- 공급자 이름(Google / 카카오)과 공급자가 준 **표시용 정보**
- `providerSubject` 원문은 보여주지 않는다

같은 공급자의 **다른 계정**도 별개 사용자다. 이것도 여기서 걸러진다.

### 3.6 빈 계정에 올린 뒤 게스트 영역

**활성 게스트를 비운다.** 남겨 두면 로그아웃했을 때 같은 기록이 게스트로 다시
나타나고, 다음 계정에 또 올라간다.

---

## 4. 평시 동기화

### 4.1 전체 스냅샷을 서버에 쓰는 API 를 만들지 않는다

받아오는 것(전체 내려받기)은 괜찮다. **조건 없이 통째로 올리는 것**만 금지한다.
`initialize-if-empty` 는 조건이 붙은 예외다.

### 4.2 수집은 추가만 있고 멱등이다

```
POST /collections/{scratchUnitId}
  이미 있으면: 서버의 기존 레코드를 유지하고 200 (오류가 아니다)
  없으면: 삽입
  응답: canonical 레코드 + recordRevision
```

같은 ID 가 서버와 오프라인 기기에 모두 있으면 **서버 것이 이긴다.**

> **디코더 규칙과 혼동하지 않는다.** 앱의 `decodeCollection` 은 한 파일 안에 같은
> ID 가 두 번 있으면 **더 이른 수집 시각**을 남긴다. 그것은 **손상 파일 복구
> 정책**이지 동기화 규칙이 아니다. 서버가 8/20, 로컬이 8/15 면 두 규칙의 결과가
> 반대다. **동기화에서는 서버가 이긴다.** 계정 캐시는 서버가 준 canonical
> 레코드로 교체한다.

**수집 취소는 없다.** 앱에 그런 기능이 없다. 생기면 이 계약을 다시 연다.

### 4.3 메모는 갱신이라 충돌한다

```
PUT /collections/{scratchUnitId}/memo
  If-Match: <recordRevision>
  mutationId: <durable UUID>  + payload hash
  불일치: 409 + 서버 값과 revision
  성공: canonical 레코드
```

**`mutationId` 가 없으면 정상 재시도가 409 가 된다** — 서버에 반영됐는데 응답만
유실된 경우다. 같은 키·같은 payload 는 이전 응답을 돌려준다.

클라이언트는 **아직 못 보낸 같은 ID 의 메모 변경을 최신 하나로 압축**하고,
같은 ID 에서 **collect 를 memo 보다 먼저** 보낸다.

409 를 받으면 조용히 덮지 않는다. **`로컬 값 / 서버 값 / 서버 값 사용 / 내 값으로
다시 저장`** 을 사용자에게 보여준다. 알림만 하면 outbox 가 영원히 멈춘다.

`memo: null` 은 **삭제**다. **필드 누락은 400** 이다.

### 4.4 내려받기와 outbox 의 순서

계정마다 **하나의 reducer** 를 둔다. 순서를 정하지 않으면 방금 긁은 것이 사라진다.

```
서버 스냅샷을 base 로 두고, 아직 못 보낸 outbox 를 그 위에 다시 재생한 결과를
원자 저장한다.
```

193개 규모라 이걸로 충분하다. delta sync 나 WebSocket 을 넣지 않는다.

### 4.5 영구 거부된 항목

서버가 **모르는 ID** 라 영구 거부하면 outbox 에서 무한 재시도하지 않는다.
**dead-letter 로 로컬에 보존**하고 표시에서 뺀다. 지우지 않는다.

### 4.6 서버 검증은 앱과 같은 규칙으로

- `collectedAtUtc` 는 **시간대가 붙은** UTC
- `utcOffsetMinutes` 는 범위 검사
- 메모는 **80 자소 클러스터** 상한, 한 줄로 정규화
- `scratchUnitId` 는 영숫자. **자릿수를 검사하지 않는다**

---

## 5. 카탈로그

### 5.1 목록을 서버에 손으로 만들지 않는다

원본은 앱 저장소다. 서버에 세 번째 사본을 손으로 만들면 조용히 어긋난다 — 이
프로젝트는 병합 명세가 3중 복제됐던 전례가 있다.

### 5.2 원본은 "현재 지도" 가 아니라 **append-only ID registry** 다

**현재 지도만으로는 폐지된 ID 의 옛 이름·시도를 복원할 수 없다.** 그런데 5.4 는
그 ID 를 영구 보존하라고 한다. 그래서 원본을 둘로 나눈다.

| 파일 | 성격 |
|---|---|
| **ID registry** (앱 저장소, 사람이 관리) | **append-only.** 한 번 들어온 ID 는 지우지 않고 `retired` 로 바꾼다 |
| 지도 에셋 | 현재 active 집합 |

생성기는 **지도의 ID 집합이 registry 의 active 집합과 정확히 같은지 검증**하고,
앱 에셋과 서버 manifest 를 **함께** 만든다.

```json
{
  "version": "2026-08-20",
  "hash": "<canonical hash>",
  "units": [{"id": "26000", "name": "부산광역시", "sido": "부산광역시", "status": "active"}]
}
```

백엔드가 별도 저장소이므로 이 manifest 를 **빌드 산출물로 복사**하되, CI 가
**원본 커밋과 SHA-256 을 검증**한다. 검증이 붙으면 "조용한 복제" 가 아니다.

### 5.3 canonical hash 의 규격을 고정한다

Python·Dart·Java 가 **같은 값**을 내야 한다. 말로만 "정렬된 해시" 라고 하면 갈린다.

- 인코딩 **UTF-8**, 유니코드 **NFC** 정규화
- `id` 오름차순 정렬 (코드포인트 기준)
- 필드 순서 `id, name, sido, status` 고정
- **null 금지**
- JSON 은 공백·개행 없이, 키 정렬
- 해시는 **SHA-256**

`version` 은 불변 문자열이다. 같은 날 두 번 개편하면 날짜만으로는 충돌하므로
**version 과 hash 는 일대일**이어야 한다. 어기면 생성기가 실패한다.

### 5.4 폐지된 ID

`status: retired` 로 남긴다. **ID 를 재사용하지 않는다** — 폐지된 ID 를 다른 단위에
다시 붙이면 과거 기록이 엉뚱한 지역으로 옮겨간다.

구버전 앱이 retired ID 로 새 수집을 보내면 **그 요청이 선언한 카탈로그 버전에서
active 였으면 받는다.** 어느 버전에서도 모르는 ID 만 영구 거부한다(4.5).

### 5.5 카탈로그 버전과 기존 v1 레코드

수집 레코드에 **어느 카탈로그 버전에서 수집했는지**를 함께 남긴다. 현재 로컬
스키마 v1 에는 그 필드가 없다. **실사용자가 0명이므로 단순하게 간다.**

- 현재 active ID 인 v1 레코드 → **첫 S3 카탈로그 버전으로 승격**
- 현재 카탈로그가 모르는 v1 ID → **`legacy-unknown`** 으로 보존. 거짓 버전을 붙이지 않는다

---

## 6. 인증

### 6.1 범위

**Google + 카카오 소셜 로그인만.** 이메일/비밀번호는 **넣지 않는다**(2026-08-21
사용자 결정으로 2026-08-16 의 병행 결정을 바꿨다).

그래서 이 앱은 **비밀번호를 저장하지 않고 메일도 보내지 않는다.** 해싱·재설정
메일·이메일 인증·계정 열거 방지가 통째로 범위 밖이다.

### 6.2 서버가 신원을 직접 검증한다

`(provider, providerSubject)` 는 DB 키로는 맞지만, **앱이 보낸 `providerSubject`
를 그대로 믿으면 인증이 아니다.**

- 서버가 공급자의 **authorization code 또는 검증 가능한 토큰에서 직접** subject 를 뽑는다
- **`providerSubject` 를 요청 본문으로 받지 않는다**
- PKCE · `state` · `nonce` · issuer · audience · 만료를 검증한다
- **암호·OAuth 를 직접 발명하지 않는다.** Spring Security 와 공급자 공식 검증 경로를 쓴다
- 공급자 토큰은 **로그인 증명 뒤 보관하지 않는다** — 지금 기능에 필요 없다

### 6.3 신원은 이메일이 아니다

**이메일로 계정을 자동 연결하지 않는다.** 공급자 이메일은 바뀔 수 있고 검증
여부도 공급자마다 다르다. 같은 이메일이라고 묶으면 **계정 탈취 경로**가 된다.

카카오는 이메일을 주지 않을 수 있다. **이메일을 필수로 요구하지 않는다.**

### 6.4 계정 연결은 이번 범위 밖이다

Google 로 만든 계정과 카카오로 만든 계정은 **별개 사용자**다. 같은 공급자의 다른
계정도 별개다. 한 사람이 둘 다 쓰면 기록이 나뉜다.

이 제약을 **로그인 화면에 적고**, 3.5 의 계정 확인 화면으로 한 번 더 거른다.

### 6.5 세션 토큰

여기서 말하는 refresh token 은 **앱 자체 세션 토큰**이지 공급자 토큰이 아니다.

- 사용자 식별은 **인증 principal 에서만**. 요청 본문의 사용자 ID 를 믿지 않는다
- **Android Keystore 기반 secure storage** 에 둔다
- **`app_flutter/` 에 평문으로 두지 않는다** (7절)
- 서버는 **해시로** 저장하고 회전하며 재사용을 탐지한다
- **회전은 정상 사용자를 끊을 수 있다.** 두 요청이 동시에 갱신하거나 응답만
  유실되면 정상 재시도가 "탈취" 로 보인다. **클라이언트는 single-flight 로
  갱신**하고, 서버는 트랜잭션 안에서 처리하며, **직전 토큰의 짧은 유예**를 둔다
- 사용자 0명 규모에서는 JWT 보다 **DB 조회형 opaque session** 이 단순하고 안전하다

---

## 7. 로컬 파일과 OS 백업

**로컬 수집 파일 전부를 자동 백업에서 제외한다** — 게스트·계정 캐시·outbox·
세션 토큰 모두. `cloud-backup` 과 `device-transfer` 양쪽이다.

**2026-08-21 사용자 결정.** 게스트를 백업에 남겨 두면 삭제 **전**에 만들어진
백업이 재설치·기기 이전으로 복원돼 **게스트가 부활하고 다른 빈 계정에 다시
올라간다.** 앱이 OS 백업본을 지운다고 보장할 수 없으므로, "완전 삭제" 를 말 그대로
지키려면 백업 자체를 막아야 한다.

**대가는 8절이다** — 비로그인 사용자는 앱을 지우거나 기기를 바꾸면 기록을 잃는다.

실제 변경은 `backup_rules.xml` 과 `data_extraction_rules.xml` 에서 하며 **N9 의
완료 조건**이다. 지금은 `settings.json` 만 제외돼 있다.

---

## 8. 게스트의 보존 수준

브리프의 성공 기준은 "앱 삭제·기기 변경 후에도 보존" 이었다. **7절 결정으로
게스트에게는 이것이 성립하지 않는다.**

| | 보존 |
|---|---|
| 로그인 사용자 | **서버로 보장** |
| 게스트 | **보존되지 않는다.** 앱을 지우거나 기기를 바꾸면 사라진다 |

**성공 기준을 "로그인하면 보존된다" 로 좁혔다**(2026-08-21). 브리프에 반영했다.

그래서 **앱이 이 사실을 알려야 한다.** 어느 정도 모은 사용자에게 로그인을
권하는 안내가 필요하다 — 강요하지 않되, 모르고 잃는 일은 없어야 한다.
문구와 시점은 N10 에서 정한다.

---

## 9. 계정 삭제

법규가 요구하고 Google Play 도 요구한다. **인증 단계의 완료 조건**이다.

지우는 것: 소셜 신원 · 모든 세션 · 수집 레코드 · 메모 · (메달 레코드가 생기면
그것도) · 기기의 계정 캐시와 outbox 와 dead-letter.

경계:

- **온라인 + 최근 재인증**을 요구한다
- 삭제 전에 **동기화와 진행 중 요청을 먼저 멈춘다**
- 삭제 뒤 늦게 도착한 outbox 요청이 **계정을 되살리지 못하게** `accountGeneration`
  을 둔다
- 같은 소셜 신원으로 다시 로그인하면 **새 빈 계정**이 된다. 옛 데이터는 돌아오지 않는다
- **공급자 계정을 잃으면 신원을 증명할 방법이 없다.** 이메일도 계정 연결도 없기
  때문이다. 이 한계를 개인정보처리방침에 **명시**한다
- DB 덤프에서 언제 사라지는지를 보존 기간으로 적는다

> 메달은 지금 수집 수에서 **계산**할 뿐 레코드가 없다. 영구화 여부는 아직 미결이라
> (11절) "생기면" 으로 적는다.

---

## 10. 위협 모델과 로그

- **남의 기록을 읽을 수 없다.** 모든 조회를 인증 principal 로 좁힌다. **테스트로 못박는다**
- **수집 기록 자체가 개인정보다.** 어느 지역에 언제 갔는지와 메모는 여행 이력이다
- **로그에 남기지 않는 것**: 토큰, 메모 내용, 요청 본문 전체
- 로그인·토큰 갱신에 rate limit
- **서버 DB 손실 = 전 사용자 기록 손실.** 로컬을 지우므로 서버가 유일본이다.
  자동 백업과 **복원 훈련**이 선택이 아니다

**DB 덤프는 Oracle Object Storage 에 둔다**(2026-08-21 사용자 결정). 같은 VM
안의 덤프는 디스크 손상·인스턴스 삭제·계정 침해에 **함께 사라져** 백업이 아니다.

- 무료 한도 안에서 쓸 수 있는지 **먼저 확인한다**. 안 되면 다른 장애 영역으로 옮긴다
- 덤프는 **암호화**한다
- 보존 기간을 정하고 **계정 삭제가 덤프에 언제 반영되는지**를 방침에 적는다
- **복원을 실제로 한 번 해 본다.** 출시 전 필수다

---

## 11. 결정 기록과 남은 미결

### 서명과 공급자 등록 (2026-08-21 결정)

**Play App Signing 을 쓴다.** Google 이 서명키를 관리하므로 키를 잃을 위험이 없다.

**대신 최종 인증서 지문은 앱을 Play 에 한 번 올려야 나온다.** 그래서 순서가 이렇다.

1. `applicationId` 를 확정한다 (현재 `kr.bangbang.mapscratch` 에 TODO 주석이 붙어 있다)
2. **개발용 지문으로 먼저 붙여** 로그인 흐름을 완성한다
3. Play 에 올린 뒤 **최종 지문으로 콘솔 등록을 갱신한다** — 이 단계를 잊으면
   출시본에서만 로그인이 깨진다. N12 체크리스트에 넣는다

아직 정할 것: native SDK 콜백인지 서버 HTTPS 콜백인지, 카카오 키 해시,
**무료 서브도메인이 Google 동의 화면 도메인 검증을 통과하는지** — N3 스파이크에서 본다.

무료 서브도메인의 실제 위험은 이름 회수·DNS 계정 탈취·서비스 종료로 **API 주소와
콜백을 함께 잃는 것**이다. 앱 업데이트 없이는 주소를 못 바꾸므로 DNS 계정에
MFA 를 걸고 변조를 감시한다.

### 남은 미결 — 메달 영구화 (Medium)

게스트도 영구화할지, 기존 계산 메달의 획득 시각을 무엇으로 채울지. **핵심 보존
성공 기준은 메달 없이도 충족**되므로 S3 핵심 동기화 뒤로 미룬다.

### 남은 미결 — 개인정보처리방침 게시 위치 (Medium)

도메인을 사지 않으므로 무료 페이지를 쓴다. **실제 계정 데이터를 받는 테스트
전에** 게시해야 한다.

---

## 12. 작업 순서

Codex 권고를 반영해 의존성을 앞으로 당겼다.

```
N1 계약 확정 (이 문서)
N2 카탈로그 registry·manifest·hash 규격        ← DB 스키마가 여기 기댄다
N3 서명·패키지 ID·OAuth 흐름·서브도메인 스파이크 ← 콘솔 등록 전에 필요하다
N4 서버 뼈대 (Spring Boot · Postgres · Flyway · TLS)
N5 DB 모델 (사용자 · 소셜 신원 · 세션 · 수집 · 카탈로그 버전)
N6 Google 로그인 + 계정 삭제                    ← 삭제가 완료 조건이다
N7 카카오 로그인
N8 동기화 API
N9 앱 저장 구조 (계정별 영역 · outbox · secure storage · 백업 제외)
N10 앱 로그인 UI · 정산 · 경고 화면
N11 동기화 연동 · 다기기/오프라인 테스트
N12 개인정보처리방침 게시 · 백업 복원 훈련 · 배포
N13 메달 영구화 (미룰 수 있다)
```

**Google 을 수직으로 먼저 완성하고** 카카오를 같은 adapter 계약으로 붙인다.
